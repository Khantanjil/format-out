#include <iostream>


using namespace std;

int main(){

    int slices;
    cout << "Type an number: ";
    cin >> slices;
    printf("The number of the pizza slices is %i\n", slices);
    return 0;
}
